package common

// Direction is a command/guidance from the Py Composer to do something
// In the front end this would manifest as Py telling Go to send a command up the websocket to the UI
// to then do something
type Direction struct {
}
